# 环境变量样例 —— 复制成 .env 再填真实值
# .env 已在 .gitignore 里，不会入库。
#
# 生成随机密钥：python -c "import secrets; print(secrets.token_urlsafe(48))"

# ── 认证（必填）────────────────────────────────────────────
# 必须与 orchestrator-agent / rag-agent 的 AUTH_SECRET **完全相同**（共享验签）。
# ⚠️ 未配置时服务会拒绝启动（fail-closed）。
AUTH_SECRET=

# ── 模型服务 ───────────────────────────────────────────────
DEEPSEEK_API_KEY=
